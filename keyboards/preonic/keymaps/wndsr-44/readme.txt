Changes from Default keymap

- Swapped Backspace and Del location on all standard layouts
- Attempting to add Caps Word
    - Changes to config.h to enable this, including tapping term of 200ms
- added Navigation layer
- added navigation layer into tap hold for esc
    - LT(layer, kc)